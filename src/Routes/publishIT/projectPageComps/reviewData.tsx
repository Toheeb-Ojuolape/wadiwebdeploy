import { InReviewList, tableDataList } from "./dataTest";
import { ProjectComps } from "./projectComps";

export const ReviewData = (props: any) => {
  return <ProjectComps data={InReviewList} />;
};
